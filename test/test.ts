describe('Test #1', () => {
    it('can multiply', () => {
        const result = 3 * 6;
        expect(result).toBe(18);
    });
    it("can sum", () => {
        const result = 2 + 2;
        expect(result).toBe(4);
    });
})

